# Math ChackChack MVP v0.1 Foundation

이 증분은 Stage 7 기준을 제품 코드로 옮기기 위한 2D Reference Vertical Slice다.

## 검증

```bash
python -m unittest discover -s tests -v
python scripts/serve.py
```

## 핵심 경로

- `docs/product/MVP_v0.1_SCOPE.md`
- `docs/adr/`
- `contracts/`
- `apps/web/`
- `tests/`

Gate 0은 원본 10종 바이트 동기화 전까지 `FAIL`이다.
