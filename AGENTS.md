# 수학착착 제품화 작업 규칙

이 저장소의 모든 제품화 작업은 아래 순서를 지킨다. 선행 Phase의 필수 산출물과 자동 검사가 통과하지 않으면 다음 Phase를 완료로 표시하지 않는다.

1. 프로젝트 기반과 역할별 폴더
2. 요구사항 정의
3. 기능 설계
4. 인프라 설계
5. 데이터베이스 설계
6. 화면·브랜드·디자인 시스템 설계
7. 기능 개발과 기능별 단위 테스트
8. 스테이징 배포
9. 통합 테스트와 수정
10. 운영·유지보수

## 역할별 경계

- `client/`: 학생·학부모가 사용하는 웹 클라이언트 코드
- `developer/`: API·도메인·개발 도구·서버 코드
- `agent/`: AI 튜터 행동 계약·프롬프트·안전 규칙
- `docs/client/`: 요구사항·화면·사용자 승인 문서
- `docs/developer/`: 기존 디자인 원본과 개발·아키텍처 문서
- `docs/agent/`: 기존 SSOT 보조 문서와 에이전트 인계 자료
- `docs/productization/`: Phase 상태·검증·완료 보고서

## 국제화 규칙

- 필수 로케일은 `ko`, `zh-CN`, `ja`, `en`, `es`, `fr`, `it`, `ru`다.
- URL 로케일 → 저장된 사용자 설정 → 브라우저 언어 → `en` 순서로 결정한다.
- 사용자 표시 문구를 컴포넌트에 직접 작성하지 않고 번역 키를 사용한다.
- 누락 키와 미지원 로케일은 `en`으로 안전하게 대체하고 진단 이벤트를 남긴다.
- 언어뿐 아니라 숫자·날짜·시간대·복수형·텍스트 확장·RTL 추가 가능성을 고려한다.

## 브랜드·디자인 규칙

- 브랜드는 `수학착착 / MATH CHAKCHAK`, 슬로건은 `수학이 착착, 공식이 척척.`이다.
- 제품은 공식과 식이 어려운 초등 고학년을 위한 성장형 1:1 AI 수학 튜터다.
- 성격은 친근함·정확함·인내심·성취감이며 비난·과장·정답 직접 노출을 금지한다.
- 디자인은 Bento 50% + Material 35% + Playful 15%를 기준으로 한다.
- 기존 착착이·공식이 승인 자산과 해시를 보존하고 미승인 자산을 제품에 승격하지 않는다.
- 본문·제목·레이블은 `text-align: start`와 논리적 여백 속성을 기본으로 하며, 행동 버튼은 중앙 정렬하고 선택지·입력 설명은 시작 정렬한다.
- 한국어는 단어 단위 줄바꿈(`word-break: keep-all`)과 제목 균형 배치, 본문 자연 배치를 적용해 한 글자 고립·문장 잘림·카드 간 기준선 불일치를 금지한다.
- 화면 완료 증거에는 최소 1440px 데스크톱과 390px 모바일의 실제 브라우저 검토를 포함하고, 가로 넘침·텍스트 잘림·터치 대상·초점·8개 필수 로케일 확장을 확인한다.

## 품질 게이트

- 각 기능은 요구사항 ID, 기능 체크리스트, 단위 테스트를 함께 가진다.
- 린트·타입 검사·단위 테스트·국제화 검사가 통과해야 Phase 보고서를 완료로 표시한다.
- 배포는 스테이징에서 먼저 수행하고 통합·접근성·성능·보안 테스트 후 운영 승인을 받는다.
- 비밀번호·토큰·복구 코드·개인 연락처를 저장소에 기록하지 않는다.
- 자동 검사 PASS와 제품 책임자 수동 승인을 구분한다.

## Phase 보고 형식

각 Phase 완료 시 `docs/productization/reports/PHASE_N_*.md`에 목표, 산출물, 실행 검사, 실패와 수정, 차단 조건, 다음 Phase 진입 여부를 기록한다.
